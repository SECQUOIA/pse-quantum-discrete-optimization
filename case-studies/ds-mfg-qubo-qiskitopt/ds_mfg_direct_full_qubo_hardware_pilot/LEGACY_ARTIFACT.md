# Legacy Artifact Notice

This directory is a quarantined legacy hardware artifact. It records a real
`ibm_fez` run collected with the original hand-built direct-QAOA circuit path,
before the hardware handoff script switched to
`QiskitOpt.QAOA.fixed_parameter_circuit`.

Do not treat these cached counts as reproducible from the current script. Use
them only as descriptive historical evidence, and rerun the direct hardware
pilot with the current script before comparing hardware results against the
high-read noiseless QiskitOpt reference.
